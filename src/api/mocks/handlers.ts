// MirageJS centraliza as rotas no arquivo server.ts.
// Este arquivo existe apenas para manter a estrutura solicitada e pode ser usado
// futuramente para compartilhar handlers ou middlewares entre ambientes.
export { makeServer } from './server';
